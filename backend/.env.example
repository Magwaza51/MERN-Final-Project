# Environment Variables - Sample Configuration
# Copy this to .env and replace with your actual values

PORT=5000

# MongoDB Configuration
# Local MongoDB (for development)
MONGODB_URI_LOCAL=mongodb://localhost:27017/mern-project

# MongoDB Atlas (for production/cloud)
# REPLACE WITH YOUR ACTUAL ATLAS CONNECTION STRING:
# Get this from: Atlas Dashboard → Databases → Connect → Connect your application
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.xxxxx.mongodb.net/healthconnect?retryWrites=true&w=majority

# Atlas credentials (for reference and additional tools)
ATLAS_USERNAME=YOUR_USERNAME
ATLAS_PASSWORD=YOUR_PASSWORD

# Database Settings
DB_NAME=healthconnect
NODE_ENV=development
CLIENT_URL=http://localhost:3000

# Security
JWT_SECRET=your_jwt_secret_key_here_make_it_more_secure_2024
SESSION_SECRET=your_session_secret_key_here

# Email Configuration (for appointment notifications)
EMAIL_USER=healthconnect.demo@gmail.com
EMAIL_PASS=your_app_password_here
EMAIL_SERVICE=gmail

# Example of what your real .env should look like:
# MONGODB_URI=mongodb+srv://healthuser:SecurePass123@healthconnect.abc123.mongodb.net/healthconnect?retryWrites=true&w=majority
# ATLAS_USERNAME=healthuser
# ATLAS_PASSWORD=SecurePass123